import Values     from './_examples/Values'
import ValuesSrc  from './_examples/Values.tsx?raw'
import SeeAlso    from '@/site/SeeAlso.jsx'
import { Example } from '@abw/badger-website'
import { FormHiddenLink } from '@/site/Links.jsx'

## `values`

The `values` property can be used to pre-define values
for fields in the form.  This is typically used when editing existing
data records.

<Example
  Component={Values}
  code={ValuesSrc}
  caption="fields"
  highlightLines="4-7,10"
  expand
/>

<SeeAlso
  links={[
    FormHiddenLink
  ]}
/>
