import React      from 'react'
import Fields     from './_examples/Fields'
import FieldsSrc  from './_examples/Fields.tsx?raw'
import Grid       from './_examples/Grid'
import GridSrc    from './_examples/Grid.tsx?raw'
import { FieldsLink } from '@/website/site/Links.jsx'
import { Example }    from '@abw/badger-website'

## `fields`

The `fields` property can be used as a shortcut to render a set of named
fields using the <FieldsLink/> component.

<Example
  Component={Fields}
  code={FieldsSrc}
  caption="fields"
  expand
/>

## `grid`, `gap` and `stack`

Any other properties are forwarded onto the <FieldsLink/> component. For
example, you can set the `grid`, `gap` and `stack` properties to set the
layout to implement a grid layout.

<Example
  Component={Grid}
  code={GridSrc}
  caption="Fieldset Grid"
  expand
/>
