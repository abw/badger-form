import React     from 'react'
import Inline    from './_examples/Inline'
import InlineSrc from './_examples/Inline.tsx?raw'
import { Example } from '@abw/badger-website'

## `inline`

You can set the `inline` property on a field if you want the input to have
fluid width.

<Example
  Component={Inline}
  code={InlineSrc}
  caption="inline"
  undent={2}
  expand
/>


