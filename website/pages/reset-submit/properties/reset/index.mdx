import React         from 'react'
import Reset         from './_examples/Reset'
import ResetSrc      from './_examples/Reset.tsx?raw'
import { Example }   from '@abw/badger-website'
import { ResetLink } from '@/site/Links.jsx'

## `reset`

The `reset` property can be used to pass properties to the <ResetLink/>
component.

<Example
  Component={Reset}
  code={ResetSrc}
  caption="reset"
  undent={2}
  expand
/>

