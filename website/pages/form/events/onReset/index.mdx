import OnReset      from './_examples/OnReset'
import OnResetSrc   from './_examples/OnReset.tsx?raw'
import SeeAlso      from '@/site/SeeAlso.jsx'
import { FormOnErrorLink, FormOnSubmitLink, FormOnSuccessLink } from '@/site/Links.jsx'
import { Example } from '@abw/badger-website'

## `onReset`

The `onReset` property can be used to define a function
that will be called when the form is reset.

<Example
  Component={OnReset}
  code={OnResetSrc}
  caption="onReset"
  highlightLines="9-11"
  expand
/>

<SeeAlso
  links={[
    FormOnSubmitLink,
    FormOnSuccessLink,
    FormOnErrorLink
  ]}
/>
