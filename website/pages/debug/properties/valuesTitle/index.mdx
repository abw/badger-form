import React       from 'react'
import Title       from './_examples/Title'
import TitleSrc    from './_examples/Title.tsx?raw'
import { Example } from '@abw/badger-website'

## `valuesTitle`

You can use the `valuesTitle` property to change the title for the values
table.

<Example
  Component={Title}
  code={TitleSrc}
  caption="valuesTitle"
  undent={2}
  expand
/>

