import React       from 'react'
import Text        from './_examples/Text'
import TextSrc     from './_examples/Text.tsx?raw'
import { Example } from '@abw/badger-website'

## `text`

You can use the `text` property to change the text that's displayed on the
button.

<Example
  Component={Text}
  code={TextSrc}
  caption="text"
  undent={2}
  expand
/>

