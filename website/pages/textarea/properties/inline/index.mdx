import React       from 'react'
import Inline      from './_examples/Inline'
import InlineSrc   from './_examples/Inline.tsx?raw'
import { Example } from '@abw/badger-website'
import { TextAreaInputColsLink } from '@/website/site/Links.jsx'

## `inline`

You can set the `inline` property on a field if you want
the input to have fluid width.  <TextAreaInputColsLink/> property
can be used to set the width as the number of columns.  The default width
is 20.

<Example
  Component={Inline}
  code={InlineSrc}
  caption="inline"
  undent={2}
  expand
/>


