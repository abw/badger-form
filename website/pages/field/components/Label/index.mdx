import React         from 'react'
import Label         from './_examples/Label'
import LabelSrc      from './_examples/Label.tsx?raw'
import { Example }   from '@abw/badger-website'
import { FieldLink } from '@/site/Links.jsx'

## `Label`

The `Label` component can be used to add a label to a <FieldLink/>. It's
usually added automatically, but it's useful if you're implementing your own
layout for a field.

<Example
  Component={Label}
  code={LabelSrc}
  caption="Label"
  expand
/>

