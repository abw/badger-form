import React    from 'react'
import Type     from './_examples/Type'
import TypeSrc  from './_examples/Type.tsx?raw'
import { Example } from '@abw/badger-website'
import { FieldTypeLink } from '@/website/site/Links.jsx'

## `type`

The <FieldTypeLink/> property can also be set to one of the other
input types that are based on the text input: `number`,
`date`, `password`, `color` and so on.

<Example
  Component={Type}
  code={TypeSrc}
  caption="type"
  undent={2}
  expand
/>


