import React              from 'react'
import RequiredMessageSrc from './_examples/RequiredMessage.tsx?raw'
import RequiredMessage    from './_examples/RequiredMessage'
import SeeAlso            from '@/site/SeeAlso.jsx'
import TryItOut           from '@/site/TryItOut.jsx'
import { Example, Split }    from '@abw/badger-website'
import {
  FieldLink, FieldRequiredLabelLink, FieldRequiredLink,
  FieldShowRequiredLink, FieldValidateOnBlurLink, FieldValidateOnChangeLink,
  FormLink
} from '@/site/Links.jsx'

## `requiredMessage`

<Split>
  <div>
    You can use the `requiredMessage` property to change the error message
    that is generated for <FieldRequiredLink/> fields that aren`t completed.

    You can set it on individual <FieldLink/> components or on the parent
    <FormLink/> to act as the default for all fields.
  </div>
  <TryItOut>
    Try submitting the form without entering any values.  You should
    see the form-wide `requiredMessage` displayed for the
    first field, and the field-specific value for the second.
  </TryItOut>
</Split>

<Example
  Component={RequiredMessage}
  code={RequiredMessageSrc}
  caption="requiredMessage"
  highlightLines="5,12"
  expand
/>

<SeeAlso
  links={[
    FieldRequiredLink,
    FieldShowRequiredLink,
    FieldRequiredLabelLink,
    FieldValidateOnChangeLink,
    FieldValidateOnBlurLink
  ]}
/>
