import React       from 'react'
import Errors      from './_examples/Errors'
import ErrorsSrc   from './_examples/Errors.tsx?raw'
import TryItOut    from '@/site/TryItOut.jsx'
import { Example, Split } from '@abw/badger-website'

# Errors

<div className="intro">
  The `Errors` component can be used to display any form
  validation errors.
</div>

<Split>
  A typical example is shown below.
  <TryItOut>
    Try submitting the form without entering anything.
  </TryItOut>
</Split>

<Example
  Component={Errors}
  code={ErrorsSrc}
  caption="Errors"
  expand
/>
