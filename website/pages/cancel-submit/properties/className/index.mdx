import React        from 'react'
import ClassName    from './_examples/ClassName'
import ClassNameSrc from './_examples/ClassName.tsx?raw'
import { Example, Split }  from '@abw/badger-website'
import { CancelGapLink, CancelSpaceLink } from '@/site/Links.jsx'

## `className`

The `className` property can be set to define your own CSS class for the
container.  When this is defined the <CancelGapLink/> and <CancelSpaceLink/>
properties have no effect.

<Example
  Component={ClassName}
  code={ClassNameSrc}
  caption="className"
  undent={2}
  expand
/>

