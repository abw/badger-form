import React     from 'react'
import Switch    from './_examples/Switch'
import SwitchSrc from './_examples/Switch.tsx?raw'
import { Example } from '@abw/badger-website'

## `switch`

You can add the `switch` property to render the checkbox as a switch.  The
`round` and `square` properties can be added to change the style.

<Example
  Component={Switch}
  code={SwitchSrc}
  caption="switch"
  undent={2}
  expand
/>


