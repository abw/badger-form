import React       from 'react'
import Values      from './_examples/Values'
import ValuesSrc   from './_examples/Values.tsx?raw'
import { Example } from '@abw/badger-website'

## `values`

The default behaviour is to show the form values.  If you don't
want to display the values (e.g. if you're only interested in the
form status) then you can set `values` to be false.

<Example
  Component={Values}
  code={ValuesSrc}
  caption="values"
  expand
/>

