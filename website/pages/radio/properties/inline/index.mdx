import React       from 'react'
import Inline      from './_examples/Inline'
import InlineSrc   from './_examples/Inline.tsx?raw'
import { Example } from '@abw/badger-website'

## `inline`

The default behaviour is for radio button options to be displayed as blocks
that extend to the full width of the container. You can set the `inline`
property on the field to display the options inline.

<Example
  Component={Inline}
  code={InlineSrc}
  caption="inline"
  undent={2}
  expand
/>


