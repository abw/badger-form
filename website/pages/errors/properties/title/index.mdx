import React      from 'react'
import Title      from './_examples/Title'
import TitleSrc   from './_examples/Title.tsx?raw'
import NoTitle    from './_examples/NoTitle'
import NoTitleSrc from './_examples/NoTitle.tsx?raw'
import TitleFn    from './_examples/TitleFn'
import TitleFnSrc from './_examples/TitleFn.tsx?raw'
import TryItOut   from '@/website/site/TryItOut.jsx'
import { Example, Split } from '@abw/badger-website'

## `title`
<p>
  The `title` property can be used to set the title
  for the component.
</p>
<Example
  Component={Title}
  code={TitleSrc}
  caption="title"
  undent={2}
  expand
/>
<p>
  You can set the `title` to false if you don't want it.
</p>
<Example
  Component={NoTitle}
  code={NoTitleSrc}
  caption="no title"
  undent={2}
  expand
/>
<Split>
  <p>
    The `title` property can also be a function.  It will be
    passed the number of error message so that it can generate an
    appropriate title.
  </p>
  <TryItOut>
    Try entering vowels in the fields below.  The title bar for the
    errors component will depending on there being one or more errors.
  </TryItOut>
</Split>
<Example
  Component={TitleFn}
  code={TitleFnSrc}
  caption="title function"
  expand
/>


