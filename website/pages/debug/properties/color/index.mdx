import React    from 'react'
import Color    from './_examples/Color'
import ColorSrc from './_examples/Color.tsx?raw'
import { Example } from '@abw/badger-website'

## `color`

You can use the `color` property to set the color for the table(s).  The
default value is `brand`.

<Example
  Component={Color}
  code={ColorSrc}
  caption="color"
  undent={2}
  expand
/>

