import React    from 'react'
import Cols     from './_examples/Cols'
import ColsSrc  from './_examples/Cols.tsx?raw'
import { Example } from '@abw/badger-website'
import { TextAreaInputInlineLink } from '@/website/site/Links.jsx'

## `cols`

The `cols` property can be used to set the number of columns.
This will only have any effect when the <TextAreaInputInlineLink/> property
is set.

<Example
  Component={Cols}
  code={ColsSrc}
  caption="cols"
  undent={2}
  expand
/>


