import React       from 'react'
import Space       from './_examples/Space'
import SpaceSrc    from './_examples/Space.tsx?raw'
import { Example } from '@abw/badger-website'

## `space`

The `space` property can be added to change the class to `flex space controls`.
This will space the controls to either side of the container.

<Example
  Component={Space}
  code={SpaceSrc}
  caption="space"
  undent={2}
  expand
/>

