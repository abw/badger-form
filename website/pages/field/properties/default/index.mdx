import React      from 'react'
import DefaultSrc from './_examples/Default.tsx?raw'
import Default    from './_examples/Default'
import { Example } from '@abw/badger-website'

## `default`

The `default` property can be used to define a default value for a field.

<Example
  Component={Default}
  code={DefaultSrc}
  caption="default"
  highlightLines="8"
  expand
/>
