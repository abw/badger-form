import OnError    from './_examples/OnError'
import OnErrorSrc from './_examples/OnError.tsx?raw'
import SeeAlso    from '@/site/SeeAlso.jsx'
import { FormOnSubmitLink, FormOnSuccessLink } from '@/site/Links.jsx'
import { Example } from '@abw/badger-website'

## `onError`

The `onError` property can be used to define a function
that will be called in the event of a failed submission of the
form.

<Example
  Component={OnError}
  code={OnErrorSrc}
  caption="onError"
  highlightLines="16-18,23"
  expand
/>

<SeeAlso
  links={[
    FormOnSubmitLink,
    FormOnSuccessLink
  ]}
/>
