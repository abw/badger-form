import React     from 'react'
import Border    from './_examples/Border'
import BorderSrc from './_examples/Border.tsx?raw'
import { Example } from '@abw/badger-website'

## `border`

You can set the `border` property to add a border around
the checkbox field.

<Example
  Component={Border}
  code={BorderSrc}
  caption="border"
  undent={2}
  expand
/>


