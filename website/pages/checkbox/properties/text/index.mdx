import React     from 'react'
import Text      from './_examples/Text'
import TextSrc   from './_examples/Text.tsx?raw'
import { Example } from '@abw/badger-website'

## `text`

Use the `text` property to add some text to be displayed alongside the
checkbox.

<Example
  Component={Text}
  code={TextSrc}
  caption="text"
  undent={2}
  expand
/>


