import React          from 'react'
import OnChangeSource from './_examples/OnChange.tsx?raw'
import OnChange       from './_examples/OnChange'
import SeeAlso        from '@/site/SeeAlso.jsx'
import TryItOut       from '@/site/TryItOut.jsx'
import TSNotes        from '@/ui/TSNotes'
import { Example, Split } from '@abw/badger-website'
import {
  FieldLink, FieldOnBlurLink, FieldOnFocusLink, FieldOnInvalidLink,
  FieldOnValidLink, FieldValidateOnChangeLink
} from '@/site/Links.jsx'


## `onChange`

<Split>
  You can add an `onChange` handler to a <FieldLink/> which will be called
  when the field value is changed.  The field context will be passed as an
  argument.

  <TryItOut>
    Try entering some text in the field below.  You should see the
    "Current value" display update to reflect the field
    content.
  </TryItOut>
</Split>

<Example
  Component={OnChange}
  code={OnChangeSource}
  caption="onChange"
  highlightLines="10-12"
  expand
/>

<TSNotes>
The `onFocus` function has a type of `FieldContextFunction` and receives
the field context as the first argument with a type of `FieldContextItems`.
</TSNotes>

<SeeAlso
  links={[
    FieldValidateOnChangeLink,
    FieldOnBlurLink,
    FieldOnFocusLink,
    FieldOnValidLink,
    FieldOnInvalidLink
  ]}
/>
