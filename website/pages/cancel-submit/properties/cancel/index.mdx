import React       from 'react'
import Cancel      from './_examples/Cancel'
import CancelSrc   from './_examples/Cancel.tsx?raw'
import { Example } from '@abw/badger-website'

## `cancel`

The `cancel` property can be used to pass properties to the cancel button.

<Example
  Component={Cancel}
  code={CancelSrc}
  caption="reset"
  undent={2}
  expand
/>

